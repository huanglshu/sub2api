根因已确认：Nginx 修改后请求可以抵达 `aigateway.edgecloudapp.com`，但该 OpenAI 兼容上游也拒绝 `response_format`；目前代码只对包含 `azure.com` 的 Base URL 删除该字段，因此该账号不会命中兼容处理。

1. 将当前散落的 Azure 判断收敛为图像请求的兼容性辅助函数：解析 Base URL 后按精确主机名判断。支持 `*.azure.com` 与精确的 `aigateway.edgecloudapp.com`，而不是用宽泛的字符串包含匹配，避免错误匹配伪造或无关域名。

2. 在 API Key 图像转发和账号连通性测试中统一调用该辅助函数。当上游不支持该参数时，从 JSON body 删除 `response_format`，且测试请求不再添加该字段。

3. 扩展 multipart 图片编辑请求的重写函数，让它在相同兼容条件下跳过 `response_format` 表单字段；这样 `/images/edits` 不会保留同样的无效参数。非兼容上游会保持字段和现有行为不变。

4. 添加回归测试：精确 EdgeCloud 主机删除参数、普通非 Azure/非 EdgeCloud 主机保留参数、相似但不匹配的主机名不触发删除；同时覆盖 API Key 账号测试与 multipart 编辑请求。

5. 运行图像与账号测试的聚焦 Go 测试，并报告结果。